# from django.urls import path, include
# from rest_framework.routers import DefaultRouter
# from .views import TestMailingViewSet, MailingAttachmentViewSet

# router = DefaultRouter()
# router.register(r'mailings', TestMailingViewSet, basename='testmailing')
# router.register(r'attachments', MailingAttachmentViewSet, basename='mailingattachment')

# urlpatterns = [
#     path('', include(router.urls)),
# ]